# Copy to .env and fill locally. Never commit .env.
# Empty DASHBOARD_TOKEN disables the check (loopback-trust only).
# Set a non-empty token BEFORE any SSH tunnel or Tailscale Serve/funnel.

DASHBOARD_TOKEN=
AGENTIX_DASHBOARD_HOST=127.0.0.1
AGENTIX_DASHBOARD_PORT=8112
AGENTIX_DASHBOARD_WORKDIR=
